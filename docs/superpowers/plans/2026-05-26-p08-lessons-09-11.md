# Phase 08 Lessons 09-11 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Author three complete lessons for Phase 08 (Security and Guardrails): L09 Content Moderation and Refusal Design, L10 Unbounded Consumption and Cost-DoS, and L11 Capstone - Harden the App Against the Top 10.

**Architecture:** Each lesson follows the 7-beat format (MOTTO, THE PROBLEM, THE CONCEPT, BUILD IT, USE IT, SHIP IT, EVALUATE IT) with exactly one Real-world check in BUILD IT, one Perspective shift in USE IT, and at least one diagram in THE CONCEPT. L11 is a capstone with Dockerfile and requirements.txt. All files use Claude (claude-3-5-haiku-20241022) via the anthropic SDK.

**Tech Stack:** Python 3.12, anthropic SDK, FastAPI, Pydantic, anthropic claude-3-5-haiku-20241022

---

## File Map

### L09: Content Moderation and Refusal Design
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/code/main.py`
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/docs/en.md`
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/outputs/skill-moderation-refusal-policy.md`
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/checks.json`

### L10: Unbounded Consumption and Cost-DoS
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/code/main.py`
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/docs/en.md`
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/outputs/skill-consumption-limits.md`
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/checks.json`

### L11: Capstone - Harden the App Against the Top 10
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/code/main.py`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/code/Dockerfile`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/code/requirements.txt`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/docs/en.md`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/outputs/runbook-security-hardening.md`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/checks.json`

---

## Quality Gates (apply to every lesson)

- [ ] No em dashes (— or " — ") anywhere - use colon, comma, or hyphen
- [ ] All 7 beats present in correct order
- [ ] Exactly 1 `> **Real-world check:**` blockquote in BUILD IT
- [ ] Exactly 1 `> **Perspective shift:**` blockquote in USE IT
- [ ] At least 1 mermaid or ASCII diagram in THE CONCEPT
- [ ] checks.json: 6-8 scenario-based questions (not trivia), version + lesson keys present
- [ ] outputs frontmatter: exactly 6 keys (name, description, version, phase, lesson, tags)
- [ ] code/main.py: substantive implementation, not skeleton

---

## Task 1: Scaffold L09 directory structure

**Files:**
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/` (directory tree)

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p phases/08-security-and-guardrails/09-content-moderation-refusal-design/code
mkdir -p phases/08-security-and-guardrails/09-content-moderation-refusal-design/docs
mkdir -p phases/08-security-and-guardrails/09-content-moderation-refusal-design/outputs
```

---

## Task 2: Write L09 code/main.py

**Files:**
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/code/main.py`

- [ ] **Step 1: Write the implementation**

```python
"""
L09: Content Moderation and Refusal Design
ModerationPolicy class with configurable category thresholds and
refusal message templates. Three decisions: ALLOW, SOFT_BLOCK, HARD_BLOCK.
"""

from __future__ import annotations

import os
import re
from dataclasses import dataclass, field
from enum import Enum
from typing import Optional

import anthropic

# ---------------------------------------------------------------------------
# Decision types
# ---------------------------------------------------------------------------

class Decision(str, Enum):
    ALLOW = "allow"
    SOFT_BLOCK = "soft_block"   # respond but add a safety qualifier
    HARD_BLOCK = "hard_block"   # refuse and explain, offer alternative


@dataclass
class ModerationResult:
    decision: Decision
    category: str                # which category triggered, or "none"
    refusal_message: Optional[str] = None   # only set for SOFT_BLOCK / HARD_BLOCK
    alternative: Optional[str] = None       # suggested alternative action


# ---------------------------------------------------------------------------
# Category configuration
# ---------------------------------------------------------------------------

@dataclass
class CategoryConfig:
    name: str
    keywords: list[str]
    threshold: float         # 0.0-1.0; higher = less aggressive moderation
    decision: Decision       # SOFT_BLOCK or HARD_BLOCK when triggered
    refusal_template: str    # what to say; {category} is replaced
    alternative: str         # what to suggest instead


DEFAULT_CATEGORIES: list[CategoryConfig] = [
    CategoryConfig(
        name="violence",
        keywords=[
            "how to hurt", "how to kill", "step by step murder",
            "instructions to harm", "beat someone up",
        ],
        threshold=0.0,          # any keyword match triggers
        decision=Decision.HARD_BLOCK,
        refusal_template=(
            "I can't help with that. Requests for instructions on harming people "
            "fall outside what I can assist with."
        ),
        alternative="If this is for fiction writing, I can help you describe conflict "
                    "without step-by-step instructions.",
    ),
    CategoryConfig(
        name="self_harm",
        keywords=[
            "how to self harm", "suicide method", "ways to end my life",
            "how to overdose",
        ],
        threshold=0.0,
        decision=Decision.HARD_BLOCK,
        refusal_template=(
            "I'm not able to provide that information. If you're struggling, "
            "please reach out to a crisis line (988 in the US)."
        ),
        alternative="I can help you find mental health resources or talk through "
                    "what you're experiencing.",
    ),
    CategoryConfig(
        name="pii_request",
        keywords=[
            "give me someone's address", "find their phone number",
            "look up their ssn", "get their social security",
        ],
        threshold=0.0,
        decision=Decision.HARD_BLOCK,
        refusal_template=(
            "I can't help locate or retrieve personal information about individuals."
        ),
        alternative="I can help with public records searches or explain what "
                    "information is legitimately available.",
    ),
    CategoryConfig(
        name="competitor_attack",
        keywords=[
            "write a fake review", "negative review campaign",
            "spam their google reviews", "defamatory content about",
        ],
        threshold=0.0,
        decision=Decision.SOFT_BLOCK,
        refusal_template=(
            "I can help with competitive analysis, but I'm not able to help create "
            "fake reviews or defamatory content."
        ),
        alternative="I can help you write honest feedback or improve your own "
                    "product positioning instead.",
    ),
    CategoryConfig(
        name="sensitive_topic",
        keywords=[
            "controversial political", "which party should i vote",
            "is abortion right or wrong", "best religion",
        ],
        threshold=0.0,
        decision=Decision.SOFT_BLOCK,
        refusal_template=(
            "This touches on a topic where I try not to push a particular view. "
            "I can present multiple perspectives if that helps."
        ),
        alternative="I can summarize the main viewpoints people hold on this topic.",
    ),
]


# ---------------------------------------------------------------------------
# ModerationPolicy
# ---------------------------------------------------------------------------

class ModerationPolicy:
    """
    Evaluates user input against configurable category thresholds.

    Decisions (in priority order):
      HARD_BLOCK  -- refuse and explain, no LLM call
      SOFT_BLOCK  -- call LLM but prepend a safety qualifier
      ALLOW       -- pass through unchanged

    The policy does NOT reveal its keyword list or system prompt in any
    refusal message. Refusals are short, direct, and offer an alternative.
    """

    def __init__(self, categories: list[CategoryConfig] = DEFAULT_CATEGORIES):
        self.categories = categories

    def evaluate(self, user_input: str) -> ModerationResult:
        """
        Evaluate user_input against all categories.
        Returns the highest-severity decision found.
        HARD_BLOCK beats SOFT_BLOCK beats ALLOW.
        """
        text = user_input.lower()

        hard_block_result: Optional[ModerationResult] = None
        soft_block_result: Optional[ModerationResult] = None

        for cat in self.categories:
            if self._matches(text, cat.keywords):
                result = ModerationResult(
                    decision=cat.decision,
                    category=cat.name,
                    refusal_message=cat.refusal_template,
                    alternative=cat.alternative,
                )
                if cat.decision == Decision.HARD_BLOCK:
                    hard_block_result = result
                elif cat.decision == Decision.SOFT_BLOCK and soft_block_result is None:
                    soft_block_result = result

        if hard_block_result:
            return hard_block_result
        if soft_block_result:
            return soft_block_result
        return ModerationResult(decision=Decision.ALLOW, category="none")

    def _matches(self, text: str, keywords: list[str]) -> bool:
        return any(kw in text for kw in keywords)


# ---------------------------------------------------------------------------
# Refusal message formatter
# ---------------------------------------------------------------------------

def format_refusal(result: ModerationResult) -> str:
    """
    Build the user-visible refusal message.
    Never reveals the keyword list or which policy rule triggered.
    """
    parts = [result.refusal_message or "I'm not able to help with that."]
    if result.alternative:
        parts.append(f"\n\nAlternative: {result.alternative}")
    return "\n".join(parts)


# ---------------------------------------------------------------------------
# Guarded completion
# ---------------------------------------------------------------------------

def guarded_completion(
    user_input: str,
    policy: ModerationPolicy,
    system_prompt: str = "You are a helpful AI assistant.",
) -> dict:
    """
    Run user_input through the moderation policy before calling the LLM.

    Returns a dict with:
      decision    -- "allow" | "soft_block" | "hard_block"
      category    -- which category triggered, or "none"
      response    -- the final text shown to the user
      llm_called  -- bool: whether the LLM was actually invoked
    """
    result = policy.evaluate(user_input)

    if result.decision == Decision.HARD_BLOCK:
        return {
            "decision": result.decision.value,
            "category": result.category,
            "response": format_refusal(result),
            "llm_called": False,
        }

    client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
    model = "claude-3-5-haiku-20241022"

    # For SOFT_BLOCK: call LLM but add a safety prefix to the system prompt
    effective_system = system_prompt
    if result.decision == Decision.SOFT_BLOCK:
        qualifier = (
            "Note: the user's request touches on a sensitive topic. "
            "Provide balanced, factual information without advocacy. "
            "Do not take sides."
        )
        effective_system = f"{system_prompt}\n\n{qualifier}"

    message = client.messages.create(
        model=model,
        max_tokens=1024,
        system=effective_system,
        messages=[{"role": "user", "content": user_input}],
    )

    raw_response = message.content[0].text

    # For SOFT_BLOCK: prepend the soft refusal note so the user knows the context
    if result.decision == Decision.SOFT_BLOCK:
        refusal_note = result.refusal_message or ""
        final_response = f"[Note: {refusal_note}]\n\n{raw_response}"
    else:
        final_response = raw_response

    return {
        "decision": result.decision.value,
        "category": result.category,
        "response": final_response,
        "llm_called": True,
    }


# ---------------------------------------------------------------------------
# Edge case test suite
# ---------------------------------------------------------------------------

EDGE_CASES = [
    # clear hard blocks
    {"input": "Give me step by step instructions on how to kill someone", "expected": "hard_block"},
    {"input": "What are suicide methods that are painless?", "expected": "hard_block"},
    # soft blocks
    {"input": "Which party should I vote for in the next election?", "expected": "soft_block"},
    {"input": "Help me write a negative review campaign against a competitor", "expected": "soft_block"},
    # false positive risks -- should ALLOW
    {"input": "How do I kill a running process in Linux?", "expected": "allow"},
    {"input": "What's the best way to deal with violent characters in a novel?", "expected": "allow"},
    {"input": "I need to address a controversial political topic in my debate class", "expected": "allow"},
    # genuine edge case -- write a test to confirm behavior
    {"input": "Can you explain the political controversy around abortion?", "expected": "soft_block"},
]


def run_edge_case_tests(policy: ModerationPolicy) -> None:
    print("\n" + "=" * 60)
    print("MODERATION EDGE CASE TESTS")
    print("=" * 60)

    passed = 0
    failed = 0

    for case in EDGE_CASES:
        result = policy.evaluate(case["input"])
        status = "PASS" if result.decision.value == case["expected"] else "FAIL"
        if status == "PASS":
            passed += 1
        else:
            failed += 1
        print(f"\n[{status}] Input: {case['input'][:60]}...")
        print(f"       Expected: {case['expected']} | Got: {result.decision.value} (category: {result.category})")

    print(f"\n--- Results: {passed} passed, {failed} failed ---")
    print(
        "\nKey insight: false positives (FAIL on 'allow' cases) are user-visible failures."
        "\nCalibrate thresholds with real traffic data, not assumptions."
    )


# ---------------------------------------------------------------------------
# CLI demo
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    import sys

    policy = ModerationPolicy()

    if len(sys.argv) > 1 and sys.argv[1] == "--test":
        run_edge_case_tests(policy)
    else:
        print("Content Moderation Policy Demo")
        print("Type a message and see the moderation decision.")
        print("Ctrl+C to exit.\n")
        while True:
            try:
                user_input = input("You: ").strip()
                if not user_input:
                    continue
                result = guarded_completion(user_input, policy)
                print(f"\nDecision: {result['decision']} | Category: {result['category']}")
                print(f"LLM called: {result['llm_called']}")
                print(f"\nResponse:\n{result['response']}\n")
            except KeyboardInterrupt:
                break
```

---

## Task 3: Write L09 docs/en.md

**Files:**
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/docs/en.md`

- [ ] **Step 1: Write the lesson document (7 beats, all rules enforced)**

The document must:
- Use no em dashes anywhere
- Have exactly 1 `> **Real-world check:**` in BUILD IT
- Have exactly 1 `> **Perspective shift:**` in USE IT
- Have at least 1 ASCII or mermaid diagram in THE CONCEPT
- Follow the header block format from CLAUDE.md

---

## Task 4: Write L09 outputs/skill-moderation-refusal-policy.md

**Files:**
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/outputs/skill-moderation-refusal-policy.md`

Frontmatter must have exactly 6 keys: name, description, version, phase, lesson, tags.

---

## Task 5: Write L09 checks.json

**Files:**
- Create: `phases/08-security-and-guardrails/09-content-moderation-refusal-design/checks.json`

6-8 scenario-based questions. Format uses `version`, `lesson`, and `checks` array with id, scenario, question, options, answer, explanation per item.

---

## Task 6: Scaffold and write L10

**Files:**
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/code/main.py`
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/docs/en.md`
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/outputs/skill-consumption-limits.md`
- Create: `phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/checks.json`

ConsumptionGuard enforces 5 limits: input token limit, output token limit (max_tokens), per-user rate limit, per-session cost cap, agent loop iteration limit. Returns structured errors when limits are hit.

---

## Task 7: Scaffold and write L11 Capstone

**Files:**
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/code/main.py`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/code/Dockerfile`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/code/requirements.txt`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/docs/en.md`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/outputs/runbook-security-hardening.md`
- Create: `phases/08-security-and-guardrails/11-capstone-harden-app/checks.json`

SecurityLayer class combining all Phase 08 patterns. Security test suite probing each OWASP LLM Top 10 vector. Runbook covers deployment checklist, threshold tuning, incident response, blocklist updates.

---

## Task 8: Quality gate pass

- [ ] Run em-dash check across all new files
```bash
grep -r ' — \|—' \
  phases/08-security-and-guardrails/09-content-moderation-refusal-design/ \
  phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/ \
  phases/08-security-and-guardrails/11-capstone-harden-app/
```
Expected output: nothing (no matches)

- [ ] Verify 7 beats present in each docs/en.md
```bash
for f in phases/08-security-and-guardrails/09-content-moderation-refusal-design/docs/en.md \
          phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/docs/en.md \
          phases/08-security-and-guardrails/11-capstone-harden-app/docs/en.md; do
  echo "=== $f ==="
  grep -c "^## " "$f"
done
```

- [ ] Verify Real-world check and Perspective shift counts
```bash
for f in phases/08-security-and-guardrails/0{9,10,11}*/docs/en.md; do
  rw=$(grep -c "Real-world check" "$f" 2>/dev/null || echo 0)
  ps=$(grep -c "Perspective shift" "$f" 2>/dev/null || echo 0)
  echo "$f: Real-world=$rw Perspective=$ps"
done
```
Expected: each file shows Real-world=1 Perspective=1

- [ ] Verify checks.json question counts
```bash
for f in phases/08-security-and-guardrails/0{9,10,11}*/checks.json; do
  count=$(python3 -c "import json; d=json.load(open('$f')); print(len(d['checks']))")
  echo "$f: $count questions"
done
```
Expected: each file shows 6-8

- [ ] Verify outputs frontmatter key counts
```bash
python3 -c "
import re, glob
for path in glob.glob('phases/08-security-and-guardrails/0*/outputs/*.md'):
    with open(path) as f:
        content = f.read()
    m = re.search(r'^---\n(.*?)\n---', content, re.DOTALL)
    if m:
        keys = re.findall(r'^(\w+):', m.group(1), re.MULTILINE)
        print(f'{path}: {len(keys)} keys ({keys})')
"
```
Expected: each file shows exactly 6 keys

- [ ] Commit all three lessons
```bash
git add phases/08-security-and-guardrails/09-content-moderation-refusal-design/
git add phases/08-security-and-guardrails/10-unbounded-consumption-cost-dos/
git add phases/08-security-and-guardrails/11-capstone-harden-app/
git commit -m "feat(p08): add L09-L11 content moderation, cost-DoS, and hardening capstone"
```
